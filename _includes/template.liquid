<!DOCTYPE html>
{% assign unixtimestamp = 'now' | date: '%s' %}
{% assign experience = 'now' | date: '%Y' | minus: 2017 | divided_by: 1 | ceil %}
{% assign meta_url = 'jhiltonsantos/curriculum-vitae' %}
{% assign meta_title = 'Hilton Santos Curriculo' %}
{% assign meta_image = 'https://opengraph.githubassets.com/' | append: unixtimestamp | append: '/jhiltonsantos/curriculum-vitae' %}
{% assign meta_description = 'Profissional com experiência em desenvolvimento front-end.' %}
<html lang="pt-BR">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>hiltonsantos-{{ 'now' | date: '%y-%m-%d' }}</title>
    <style>{% include "./style.css" %}</style>
    <meta name="title" content="{{ meta_title }}">
    <meta name="description" content="{{ meta_description}}">
    <meta property="og:type" content="website">
    <meta property="og:url" content="{{ meta_url }}">
    <meta property="og:title" content="{{ meta_title }}">
    <meta property="og:description" content="{{ meta_description }}">
    <meta property="og:image" content="{{ meta_image }}">
  </head>
  <body>
    <main>{{ content }}</main>
  </body>
</html>
